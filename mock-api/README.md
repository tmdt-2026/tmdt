# Mock API

Chay mock API de cap data dong cho giao dien:

```bash
npm install
npm run mock:api
```

API endpoint duoc su dung trong frontend:

- `GET http://localhost:3000/siteData`

Du lieu nguon nam trong file `mock-api/db.json`.
